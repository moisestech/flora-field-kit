# FLORA API (server-only — never expose to the client)
# Create at: https://app.flora.ai → Settings → API Keys
# Requires a paid FLORA subscription for API access.
FLORA_API_KEY=

# Force demo fixtures even when FLORA_API_KEY is set
# DEMO_MODE=1
